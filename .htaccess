<IfModule mod_rewrite.c>
   RewriteEngine on
   
   RewriteRule ^media/(.*)$ http://media.padcon-leipzig.de/$1 [L]
   
   RewriteRule    ^$ app/webroot/    [L]
   RewriteRule    (.*) app/webroot/$1 [L]
</IfModule>

AuthType Basic
AuthName "Login"
AuthUserFile .htpasswd
require valid-user 